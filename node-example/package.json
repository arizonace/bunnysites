{
  "name": "node-example",
  "version": "1.0.0",
  "private": true,
  "description": "A small Express site intended to run behind a loopback Nginx proxy.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
